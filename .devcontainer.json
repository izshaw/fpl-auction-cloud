    {
      "name": "FPL Auction Dev Environment",
      "build": { "dockerfile": "Dockerfile" },
      "forwardPorts": [3000, 5000],
      "postCreateCommand": "cd fpl_auction && sudo service postgresql start && sudo -u postgres psql -c 'CREATE DATABASE fpl_auction;' && cd backend && npm install && npx prisma migrate dev --name init && cd ../frontend && npm install",
      "remoteUser": "codespace"
    }